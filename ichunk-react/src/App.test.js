// Test file to verify the React application structure
import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

// Simple test to verify the app renders without crashing
test('renders without crashing', () => {
  render(<App />);
});


